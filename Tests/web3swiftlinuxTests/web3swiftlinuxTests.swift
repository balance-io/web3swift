import XCTest
@testable import web3swiftlinux

class web3swiftlinuxTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(web3swiftlinux().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
